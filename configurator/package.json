{
  "name": "rpc-cluster-configurator",
  "version": "0.1.0",
  "description": "Cluster configurator for RPC Cluster distributed inference",
  "author": "rpc-cluster",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build:mac": "electron-builder --mac dmg",
    "build:win": "electron-builder --win nsis"
  },
  "dependencies": {
    "adm-zip": "^0.5.10"
  },
  "devDependencies": {
    "electron": "^31.0.0",
    "electron-builder": "^24.0.0"
  }
}
